import 'package:flutter/material.dart';

class LotoListScreen extends StatelessWidget {
  const LotoListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
